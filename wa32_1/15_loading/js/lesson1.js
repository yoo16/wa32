$(function () {

    function loadImages() {
        let srcList = []; //画像の参照先を格納する配列
        let index = 0; //読み込み画像インデックス
        let duration = 800;

        function init() {
            console.log('初期化');
        };

        function load() {
        };

        function loaded() {
        };

        function complete() {
        };
        init();
    };

    loadImages();
});