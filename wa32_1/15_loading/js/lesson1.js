$(function () {

    function load() {
        let srcList = []; //画像の参照先を格納する配列
        let index = 0; //読み込み画像インデックス

        //初期設定
        function init() {
        };

        //画像の読み込み
        function loadImage() {
        };

        //画像の読み込み完了
        function loaded() {
        };

        //すべての画像の読み込み完了
        function complete() {
        };

        init();
    };

    load();

});